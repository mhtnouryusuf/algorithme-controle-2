#include <iostream>
#include <cmath>
using namespace std;

int main() {
    int p = 5; 
    long long M = (1LL << p) - 1; // M = 2^p - 1
    long long s = 4;

    for (int i = 0; i < p - 2; i++)
        s = (s * s - 2) % M;

    cout << "Test of nгmbre of Mersenne M" << p << " = " << M << endl;
    if (s == 0)
        cout << "Result : M" << p << " isfrst." << endl;
    else
        cout << "Result : M" << p << "is not frst." << endl;

    return 0;
}