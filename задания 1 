#include <iostream> 
using namespace std; 

 int pgcd( int a , int b) { 
    return  b== 0 ? a : pgcd(b , a % b); 

}   
    
    int main () {
    int a , b ,c ; 
    cout<<" between the three numbers : 15   45  75 ";
    cin >> a >> b >> c; 
    int result = pgcd(pgcd ( a , b ), c ) ; 
    cout <<" the largest divisor of three numbres is   (pgcd) the  three numbres is : 15 "; 
    return 0 ;  
   }

